import React from "react";
import { FormControlLabelProps } from "./FormControlLabel.types";

const FormControlLabel = ({ control }: FormControlLabelProps) => {
  return <div>{control}</div>;
};

export default FormControlLabel;
