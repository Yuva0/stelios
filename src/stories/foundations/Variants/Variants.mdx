import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as VariantsStories from './Variants.stories';

<Meta of={VariantsStories} name="Docs"/>

# Variants:

Variants are an important means of grouping all components that share a common visual style.\
They are used to create a consistent look and feel across the application.\
Every component has different variants for different purposes. You can see the variants of each component in the component's story.\

## Examples
Here are some examples of variants
<Canvas of={VariantsStories.Sample}/>