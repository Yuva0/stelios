{/* Colors.mdx */}
import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as ColorStories from './Colors.stories';

<Meta of={ColorStories} name="Docs"/>

# Colors:
Colors are used to convey meaning, create visual interest, and establish a brand identity.\
That is why Stelios can create entire websites even using just **one color**.

## Example
<Canvas of={ColorStories.Sample}/>