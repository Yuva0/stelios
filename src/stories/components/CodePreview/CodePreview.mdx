{/* CodePreview */}
import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as CodePreviewStories from './CodePreview.stories';

<Meta of={CodePreviewStories} name="Docs"/>
# CodePreview:
CodePreview is a component that displays code with the preview. It is used to display code snippets in documentation.

#### Default
<Canvas of={CodePreviewStories.Default} />