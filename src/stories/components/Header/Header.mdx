{/* Header.mdx */}
import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as HeaderStories from './Header.stories';

<Meta of={HeaderStories} name="Docs"/>
# Header:
Headers are used to display important information on a page. They are one of the most important element in a design system.

#### Default
<Canvas of={HeaderStories.Default} />