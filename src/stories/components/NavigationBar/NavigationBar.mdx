{/* NavigationBar.mdx */}
import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as NavigationBarStories from './NavigationBar.stories';

<Meta of={NavigationBarStories} name="Docs"/>

# Navigation Bar:
Navigation Bars are used to display a list of links.\
They are used to navigate between different pages of the website.

#### Default:
<Canvas of={NavigationBarStories.Default} />